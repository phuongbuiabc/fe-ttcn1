export enum PigletHerdMovementType {
    SPLIT = 'SPLIT',
    MERGE = 'MERGE',
    INCREASE = 'INCREASE',
    DECREASE = 'DECREASE',
}

export enum PigletHerdStatus {
    UNWEANED = 'UNWEANED',
    WEANED = 'WEANED',
}